Your next steps are:

Install dependencies.
Run:
python -m pip install -r C:\Projects\Job Bot\requirements.txt
If you want the cheapest setup, install Ollama and pull the default model.
Recommended:
ollama pull qwen2.5:7b
If you skip this, the app can still use hosted fallback models if you configure API keys.

Launch the app.
Run:
python C:\Projects\Job Bot\main.py
Fill out the Setup tab.
Set:
resume source
cheap stage provider/model
strong stage provider/model
doc stage provider/model
Anthropic and/or OpenAI keys if you want hosted stages
include/exclude titles
salary floor if you want one
automation mode = semi_auto
Save config, then run a test search.
Start with a narrow keyword/location so the first run is easy to inspect.
Review the Review Queue.
Open:
job description
generated resume
generated cover letter
Then use Approve and Send only if Gmail is configured.
My practical recommendation:

First boot: use semi_auto
Cheapest first pass: cheap stage = ollama_local / qwen2.5:7b
Strong stage: openai / gpt-5-mini or anthropic / claude-sonnet-4-20250514
Doc stage: openai / gpt-5-mini
If you want, I can also give you the exact “first-run settings” I’d use for a safe initial test.